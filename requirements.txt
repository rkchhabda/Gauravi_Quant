# Core: weekly ranking product + factor research pipeline.
# This is what CI installs (.github/workflows/weekly_ranking.yml) -- keep it lean.
pandas>=2.2
numpy>=1.26,<3
yfinance>=0.2.54
scikit-learn>=1.4
xgboost>=2.0
scipy>=1.11
requests>=2.31
# HTML table parser backend for the moneycontrol FII/DII fallback tier
# (pandas.read_html needs lxml; without it that tier raises ImportError and is skipped).
lxml>=5.0
# Web API server for dashboards - pin pydantic<2.10 to avoid Rust compilation
fastapi==0.110.1
uvicorn==0.29.0
pydantic==2.9.2
# Test suite (tests/ only -- scripts/manual/ is excluded via pytest.ini)
pytest>=8.0
